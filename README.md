[![Review Assignment Due Date](https://classroom.github.com/assets/deadline-readme-button-22041afd0340ce965d47ae6ef1cefeee28c7c493a6346c4f15d667ab976d596c.svg)](https://classroom.github.com/a/qR-atX5d)
# 🐍 [2026-01] 나만의 파이썬 소프트웨어 개발 프로젝트

## 1. 시나리오 제목
가상의 '반려동물 건강 일지'


## 2. 시나리오 (5~10줄)
반려동물 건강 일지 프로그램인 **'멍냥 헬스케어'**는 우리의 소중한 가족인 반려동물의 상태를 체계적으로 관리하는 프로그램입니다.사용자는 먼저 반려동물의 이름과 종, 목표 체중을 설정하며 시작합니다. 매일 사료 섭취량과 산책 시간(또는 활동량)을 입력하면, 프로그램은 조건문을 통해 권장량 대비 상태가 '적정'한지 아니면 '에너지 과잉'인지를 즉각 판정해 줍니다. 메인 메뉴에서는 새로운 기록을 추가하거나 그동안 쌓인 일지를 조회할 수 있고, 배열에 저장된 최근 데이터들을 바탕으로 함수가 평균 활동량을 계산해 건강 점수를 산출합니다. 단순히 점수를 매기는 성적 관리에서 벗어나, 생동감 있는 데이터로 반려동물의 건강 패턴을 한눈에 파악할 수 있는 시스템을 완성할 계획입니다.


<br>


## 3. 예상 기능 및 메뉴 (최소 5개)
1.오늘의 건강 상태 기록 (입력 및 배열 저장)
2. 실시간 건강 상태 진단 (조건문 활용)
3. 최근 7일 건강 요약 보고서 (함수 및 계산)
4.날짜별 기록 검색 (배열 탐색)
5. 목표 체중 달성도 확인 (수치 연산)
6. 프로그램 종료 및 데이터 초기화 (반복문 제어)
<br>

# 🚀 [버전별 개발 일지 & AI 협업 기록]

## 🟦 [1차 과제: V1.0] 시나리오 기획
개발내용
동물의 이름,종,하루권장 산책시간(정수형,분 단위) 등의 기초데이터 설정.
조건문을 이용하여 입력된 데이터를 바탕으로 건강상태 판별.
반복문 및 메뉴를 이용하여 사용자가 종료를 선택하기 전까지 반복.
함수 및 배열을 통해 데이터를 구조적으로 저장하고 관리.

### **🤖 AI 파트너십 과정**
프롬프트 내용:학교 과제의 조건(4단계 도구 활용)을 이용한 독창적이고 창의적인 주제 추천
적용내용:성적 관리 프로그램의 논리를 유지하면서도 독창적인 3가지 테마(건강,키오스크,식재료)를 제안하여 주제를 제시해줌.

프롬프트 내용:C언어 4단계 과정 로드맵(입출력->조건문->반복문->배열/함수)에 적합한 '반려동물 건강일지'라는 로드맵을 요청함.
적용내용:오늘의 건강 상태 기록 (입력 및 배열 저장)->실시간 건강 상태 진단 (조건문 활용)->최근 7일 건강 요약 보고서 (함수 및 계산)->날짜별 기록 검색 (배열 탐색)->목표 체중 달성도 확인 (수치 연산)->프로그램 종료 및 데이터 초기화 (반복문 제어)의 로드맵 뼈대 확립.

### **🛠️Troubleshooting&기술 회고**
문제1:input()으로 받은 값은 항상**문자열(str)** 입니다. 이를 숫자로 변환하지 않고 연산하면 에러가 발생한다.
해결:int()또는 float()함수를 사용해 명시적으로 형변환을 해줘야한다.

문제2:4단계 배열(리스트) 기능에서, 저장된 기록이 2개인데 5번째 기록을 검색하려고 할 때 발생한다.
해결:if문을 사용하여 사용자가 입력한 인덱스가 len(health_1ogs) 범위 내에 있는지 확인해야한다.

문제3: 함수 내부에서 메인 루프에 있는 리스트를 수정하려고 할 때, 리스트가 인식되지 않거나 초기화되는 경우가 생긴다.
해결: 함수를 정의할 때 매개변수(Parameter)로 리스트를 전달받아 처리하거나, global 키워드의 개념을 이해하고 적용해야한다.

    
### **📁 증빙 자료:**
  * [1차_AI협업캡처.pdf 첨부 완료] (첨부 후 링크)


<br>

## 🟩 [2차 과제: V1.0] 입출력 + 리스트 + 조건문 - 향후 작성 예정
### **✨2차 과제 내용:**
데이터 수집:input()으로 기초 정보(이름,나이 등)와 활동 데이터(산책,식사 등)를 입력받아 리스트에 저장.
가중치 연산:산책(x1.5), 식사 (x0.8), 수면 (x1.2) 등 중요도에 따른 종합 지수 산출.
등급판정:if-elif-else 구문을 활용하여 S~F까지 5단계 등급 분류.
칭호부여:and/or 논리 연산자를 사용하여 특정 조건을 만족할 시'전설의 산책왕' 등 특별 칭호 출력.

자료형 및 연산:int,float 형변환 및 가중치 계산을 위한 산술/복합 대입 연산자 활용.
리스트 및 반복문:for문을 이용한 리스트 생성 및 sum(),len(),sort()등 내장 함수 4종 이상 조작.
제어 구조:중첩 if문과 논리 연산자를 결합한 고도화된 조건문 설계.
    
### **🤖 AI 파트너십 과정**
(예시) 
가중치 기반 정밀 진단 및 칭호 시스템 고도화
프롬프트 요약:"교수님이 제시한 7가지 필수 조건(가중치 공식, S~F 등급, 논리 연산자 칭호 등)을 만족하면서 지금까지 배운 리스트, 반복문 등 다양한 코딩용어를 사용하여 코드를 짜줘.

적용내용:AI와 협업하여 단순 합산이 아닌 가중치 공식(산책*1.5+식사*0.8+수면*1.2)을 도입 함. 또한, and/or 연산자를 활용하여 특정 수치를 달성한 반려동물에게 '전설의 산책왕' 같은 특별 칭호를 부여하는 지능형 판정 로직을 실제 코드에 반영함.

학습 수준에 맞춘 문법 최적화 및 구조 설계
프롬프트 요약:"오직 해당 주차까지 배운 내용(조건문,반복문,기초 자료형, 리스트 연산)만 사용하고, 교수님이 요구한 7가지 제약 조건을 모두 충족하는 구조로 정리해 줘."
적용내용:아직 배우지 않은 함수(def)나 딕셔너리(dict) 대신, 순수 리스트 조작(append.sum,sort 등)과 중첩 if문만을 사용하여 과제 범위를 준수함. 동시에 f-string 포매팅을 적용하여 결과 출력의 완성도를 높이는 등 Ai의 기술적 자문을 거쳐 코드를 최적화 함.
    
### **🛠️ Troubleshooting & 기술 회고:**
문제1:정수와 실수 입력 시 발생하는 계산 오류
원인:반려동물의 몸무게나 운동시간을 입력받을 때, int()(정수)만 사용하면 소수점 데이터(예:5.5kg)를 입력했을 때 프로그램이 멈추는 현상이 발생함.
해결:사용자로부터 정밀한 수치를 입력받아야 하는 변수에는 float()함수를 사용하여 실수형 형변환을 수행함으로써 소수점 연산이 가능하도록 해결함.

문제2:복합 조건문(and/or) 설정 시 우선순위 혼동
원인:특별 칭호를 부여하기 위해 and연산자를 사용했는데, 조건식의 순서가 잘못되어 모든 반려동물에게 칭호가 부여되거나 혹은 아무도 부여받지 못하는 논리적 오류가 발생함.
해결:관계 연산자(>=)와 논리 연산자(and)의 우선순위를 고려하여,**'지수 180점 이상'**과 **'특정 활동량 충족'**이라는 두 조건이 모두 참(True)일 때만 실행되도록 조건식을 정교하게 수정함.

문제3: 가중치 연산 결과의 가독성 저하 
원인:가중치를 곱해 종합 건강 지수를 산출하니 결과값이 소수점 아래로 너무 길게 출력되어 리포트의 가독성이 떨어짐.
해결:파이썬의 f-string 포매팅 기술 중 하나인 {variable:.2f}문법을 활용하여, 최종 지수를 소수점 둘째 자리까지만 깔끔하게 표기하도록 개선함.
### **📁 증빙 자료:**
  * [2차_AI협업캡처.pdf 첨부 완료] (첨부 후 링크)
  * [2차과제_실행결과.jpg]
<br>

## 🟨 [3차 과제: V3.0] 무한 루프와 메뉴 시스템 (반복문) - 향후 작성 예정
### **✨3차 과제 업데이트 내용:**
  * 내용.
  * 1. 핵심 업데이트 기능
무한루프 메인 메뉴: while True 구문을 활용해 사용자가 '3. 프로그램 종료'를 선택하기 전까지 입력과 조회를 반복 수행하며, 종료 선택 시에만 break로 안전하게 프로그램이 끝나도록 구현.

함수의 정의 및 모듈화: 기존 코드를 역할에 따라 input_pet_data() (입력), calculate_health() (연산), print_health_report() (출력)의 3개 이상의 개별 함수로 완벽히 쪼개어 관리.

데이터 흐름 최적화 (인자와 반환값): 연산 함수가 리스트를 매개변수(인자)로 받아 가중치 점수를 계산한 뒤, 그 결과값을 return문으로 반환하여 출력 함수로 전달하는 유기적 흐름 구축.

메인 로직의 시각화: 메인 while문 내부를 함수 호출 위주로만 구성하여, 전체 프로그램의 흐름을 목차처럼 한눈에 파악할 수 있도록 간결화.

2. 변수 및 스코프(Scope) 관리
전역 변수(Global Variable) 활용: 반려동물의 이름, 나이, 체중, 활동 리스트를 프로그램 전체에서 공유할 수 있도록 전역 공간에 선언.

지역 변수(Local Variable) 및 global 키워드: 입력 함수 내에서 전역 변수의 데이터를 안전하게 업데이트하기 위해 global 키워드를 1회 이상 사용하고, 등급이나 칭호 등은 함수 내부의 지역 변수로 철저히 분리하여 메모리를 효율적으로 관리.
    
### **🤖 AI 파트너십 과정**
 1. 내용 1: 절차형 구조에서 함수 기반 모듈화 구조로의 개편

프롬프트 요약: "기존 2차 과제 코드를 활용하여 3차 과제 조건에 맞게 입력, 연산, 출력을 수행하는 최소 3개 이상의 개별 함수(def)로 분리하고, 메인 로직은 목차처럼 간결하게 보이도록 함수 호출 위주로 짜줘."

적용 내용: 기존에 순차적으로 실행되던 코드를 input_pet_data(), calculate_health(), print_health_report()라는 3개의 독립적인 함수로 분리함. 이를 통해 메인 while문 내부의 가독성을 높이고, 프로그램의 흐름을 목차처럼 직관적으로 파악할 수 있는 클린 코드를 구축함.

2. 내용 2: 변수 스코프(전역/지역 변수) 및 데이터 전달 메커니즘 정립

프롬프트 요약: "함수 간에 데이터를 주고받을 수 있도록 매개변수와 return 반환값을 활용해 주고, 프로그램 전체에서 쓰는 전역 변수와 함수 내부의 지역 변수 역할을 구분해서 global 키워드를 사용해 줘."

적용 내용: 프로그램 최상단에 전역 변수를 선언하고, 입력 함수 내부에서 이 데이터를 안전하게 갱신하기 위해 global 키워드를 적용함. 또한, 연산 함수에서 계산된 건강 지수 결과를 return문으로 반환받아 출력 함수의 매개변수(인자)로 넘겨주는 유기적인 데이터 흐름 체계를 완성함.
### **🛠️ Troubleshooting & 기술 회고:**
 1. 문제 1: 함수 내부에서 전역 변수 수정 시 값이 반영되지 않는 현상 (UnboundLocalError)

원인: 메인 메뉴에서 1번을 눌러 input_pet_data() 함수를 통해 반려동물의 이름과 나이를 새로 입력받았으나, 함수가 끝나고 메인 프로그램으로 돌아오면 기존에 설정해 둔 전역 변수의 초기값(빈 문자열)으로 자꾸 리셋되는 현상이 발생함. 파이썬에서는 함수 내부에서 전역 변수와 같은 이름의 변수에 값을 대입하면, 이를 전역 변수가 아닌 새로운 '지역 변수'로 취급해버리기 때문에 생긴 문제였음.

해결: 입력 함수 내부 최상단에 global pet_name, pet_age, pet_weight를 명시해 줌으로써, 파이썬에게 "이 변수들은 새로 만드는 게 아니라 바깥에 있는 전역 변수를 가져다 쓸 거야!"라고 선언하여 함수 종료 후에도 입력 데이터가 안전하게 유지되도록 해결함.

2. 문제 2: 함수 연산 결과가 다음 함수로 전달되지 않고 None이 출력되는 현상

원인: 건강 지수를 계산하는 calculate_health() 함수를 실행한 뒤 그 값을 print_health_report()에 넣었는데, 자꾸 점수가 0점이나 None으로 출력되는 논리적 오류가 발생함. 원인을 분석해 보니 계산 함수 내부에서 결과를 열심히 계산만 하고, 정작 메인 로직으로 그 값을 보내주는 return 문을 누락하여 함수가 아무것도 반환하지 않았기 때문이었음.

해결: 계산 함수 맨 마지막에 return index를 정확히 명시하여 연산된 결과값을 메인 프로그램으로 전달받았고, 이 반환값을 다시 출력 함수의 인자로 넘겨주는 올바른 데이터 파이프라인을 구축하여 해결함.
### **📁 증빙 자료:**
  * [3차_AI협업캡처.pdf 첨부 완료] (첨부 후 링크)
  * [3차과제_실행결과.jpg]
<br>

### 🟥 [4차 과제: V4.0] 모듈화 및 데이터 확장 (배열과 함수) - 🌟최종 완성 -- 향후 작성 예정
### **✨4차 과제 업데이트 내용:**
  ✨ 4차 과제 내용
이번 과제는 반려동물 건강 데이터 수집 및 상태 진단 시스템 구현으로, 사용자로부터 데이터를 입력받아 이중 리스트에 누적 저장하고, 파일 입출력과 try-except 예외 처리를 통해 안정적인 데이터 보관 시스템을 구축하는 것입니다. 특히 중첩 for문(이중 순회)을 활용하여 다수의 데이터를 표 형식으로 깔끔하게 반복 출력하고, 비정상 종료를 방지하는 지능형 콘솔 프로그램을 완성해야 합니다.

input()으로 반려동물의 이름, 산책 시간, 사료 섭취량을 입력받아 이중 리스트(2D List)에 append()로 누적 저장.

입력 단계에서 문자열 오입력을 방지하는 ValueError 처리와 종료 시 파일 저장 실패를 방지하는 IOError 처리 등 최소 2곳 이상의 예외 처리 구문 적용.

for문 내부의 중첩 for문 또는 리스트 인덱싱을 활용하여 이중 리스트에 담긴 여러 반려동물의 데이터를 표 포맷으로 출력하는 이중 순회 기능 구현.

with open() 함수를 사용하여 프로그램 종료 전 이중 리스트에 쌓인 모든 데이터를 텍스트 파일(.txt)로 안전하게 내보내는 파일 저장(Write) 로직 추가.
    
민형 님, 그동안 저와 함께 브레인스토밍부터 최종 V3.0 코드 설계까지 달려온 과정을 보고서용 양식에 맞게 깔끔하게 정리해 드릴게요.

제출하시는 문서의 격식에 맞추면서도, 우리가 실제로 나눈 대화의 핵심을 쏙쏙 골라 담았습니다.

🤖 AI 파트너십 과정
1. 아이디어 빌딩 및 주제 확정
프롬프트 요약: 명지대학교 파이썬 과제 조건인 4단계 구조(변수→조건문→반복문→함수/배열)를 만족하면서, 기존 '성적 관리 프로그램'과 난이도는 비슷하지만 더 창의적인 프로젝트 주제 추천 요청.

적용 내용: 기존의 '학생-점수' 구조를 '반려동물-건강 데이터'로 치환하는 아이디어를 채택하여, 친숙하면서도 독창적인 '우리 집 막내 건강 지킴이'라는 가상의 시나리오와 콘솔 메뉴 구조를 확정함.

2. 프로그램 아키텍처 및 V1.0 설계
프롬프트 요약: 확정된 '반려동물 건강 일지' 시나리오를 바탕으로 프로그램이 가질 최소 6가지의 예상 기능 정의와 대략적인 개발 설계(데이터 구조 및 함수 명세) 요청.

적용 내용: 기록 등록, 당일 진단, 통계 보기, 데이터 검색 등 6대 핵심 메뉴를 도출하고, 각각의 기능이 파이썬의 어떤 문법 단계와 매칭되는지 정의함. 또한 데이터를 2차원 리스트 구조로 관리하는 개발 명세서 V1.0을 정립함.

3. 최종 V3.0 명세 확장 및 코드 완성
프롬프트 요약: 4차 과제의 추가 필수 조건인 '이중 리스트 활용', '중첩 for문을 통한 이중 순회', 'with open() 파일 저장', '최소 2곳 이상의 try-except 예외 처리'를 반영한 최종 V3.0 코드 구현 요청.

적용 내용: ValueError와 IOError를 방지하는 예외 처리 로직을 배치하고, 다수의 반려동물 데이터를 표 형식으로 출력하는 중첩 루프 및 프로그램 종료 시 텍스트 파일로 누적 데이터를 내보내는 최종 V3.0 소스 코드를 완성하여 GitHub 제출 기반을 마련함.
    
민형 님, 4차 과제의 핵심 요구사항이었던 이중 리스트, 파일 저장, 예외 처리 과정에서 겪을 수 있는 실제 문제들을 바탕으로 Troubleshooting(문제 해결) & 기술 회고 섹션을 작성해 드립니다.

보고서에 바로 넣을 수 있도록 개조식으로 깔끔하게 정리했습니다.

🛠️ Troubleshooting & 기술 회고
1. 문제 1: 정수 입력 시 문자열 오입력으로 인한 프로그램 비정상 종료
원인: input() 함수로 산책 시간이나 사료 섭취량을 입력받을 때, 사용자가 실수로 문자(예: '삼십분')나 공백을 입력하면 int() 형변환 과정에서 ValueError가 발생하며 프로그램이 강제로 꺼지는 현상 발생.

해결: 숫자를 입력받는 로직을 try-except ValueError 구문이 포함된 독립된 함수(input_int)로 분리함. 잘못된 입력이 들어오면 에러 메시지를 띄우고 while 루프를 통해 올바른 숫자가 입력될 때까지 반복해서 입력받도록 수정하여 프로그램의 안정성을 높임.

2. 문제 2: 이중 리스트 출력 시 데이터가 깨지거나 한 줄로 뭉쳐서 출력되는 현상
원인: 다수의 반려동물 데이터가 담긴 이중 리스트(pet_database)를 출력할 때, 단순 print()를 사용하거나 내부 데이터의 길이가 달라 콘솔 창에서 표의 열 정렬이 삐뚤빼뚤해지고 줄바꿈이 타이밍에 맞지 않게 일어남.

해결: 중첩 for문(이중 순회)을 도입하여 바깥쪽 루프는 '행(반려동물 한 마리)'을, 안쪽 루프는 '열(이름, 산책, 사료, 상태)'을 돌도록 구조화함. 파이썬의 문자열 포매팅 문법(:<12)과 end="" 옵션을 조합하여 가독성이 높은 바둑판 배열의 표 형식으로 정렬 출력함.

3. 문제 3: 파일 저장 기능 구현 시 한글 깨짐(인코딩 에러) 및 권한 문제
원인: open() 함수로 결과 보고서 파일(.txt)을 생성하고 데이터를 쓸 때, 운영체제(OS) 기본 인코딩 설정에 따라 한글 깨짐 현상이 발생하거나, 파일 경로 및 쓰기 권한 문제로 IOError가 발생할 위험성이 존재함.

해결: 자원을 안전하게 해제해 주는 with open() 구문을 사용하고, 인코딩 옵션을 encoding="utf-8"으로 명시하여 한글 깨짐을 원천 차단함. 추가로 파일 입출력 로직 전체를 try-except IOError로 감싸서 예외 상황 발생 시 시스템이 다운되지 않고 경고 메시지를 보여주도록 방어 코드를 작성함.

📝 기술 회고 (Technical Retrospective)
배운 점: 이전 V1.0에서는 프로그램이 켜져 있을 때만 데이터가 유지되는 일회성 구조였지만, V3.0에서 이중 리스트와 with open() 파일 입출력을 결합하면서 프로그램이 종료되어도 데이터가 텍스트 파일로 영구 보관되는 '실제 소프트웨어'다운 영속성을 구현해 볼 수 있었습니다.

아쉬운 점 및 향후 개선 방향: 이번에는 파일 저장 기능만 구현("w" 모드)하여 프로그램이 켜질 때 기존 데이터를 불러오는 기능("r" 모드)이 부재합니다. 향후 V4.0에서는 프로그램 시작 시 자동으로 기존 텍스트 파일을 읽어와 이중 리스트에 복원하는 '데이터 로드 기능'을 추가하여 완성도를 더 높이고 싶습니다.
     
### **📁 증빙 자료:**
  * [4차_AI협업캡처.pdf 첨부 완료] (첨부 후 링크)
  * [4차과제_실행결과.jpg]
<br>
